package gn.k48.leetcode.hot100;

public class L240_searchMatrix {
    /*
    二维矩阵每行从左到右升序，每列从上到下升序
    因此搜索可以
    1. 找到
     */
    public boolean searchMatrix(int[][] matrix, int target) {
        int l = 0, r = matrix.length;
        while (l < r) {
            int m = l + (r - l) / 2;

        }
        return false;
    }

}
